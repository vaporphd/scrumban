# Followup

## Status

Branch `main`, in sync with `origin/main`. **Phase 0 complete. Phase 1 complete end-to-end. Phase 2 data layer landed (PR 137). Nine Phase 2 REST endpoints shipped (issues 69-73 boards CRUD + archive, plus 77 + 78 + 79 + 80 — full columns CRUD + reorder). Four Phase 2 frontend issues landed (74 boards list, 75 create-board modal, 76 archive board UI, 81 board detail view)**. Issue 81 added the first UI surface that consumes `BoardDetailRead.columns[]` from PR 142 (issue 71). New route `/boards/:id` with `requiresAuth: true` mounted on `BoardDetailView.vue`. Five UI states: loading spinner, 404 not-found (dedicated branch with back-link to `/boards`, distinct from generic error+retry), generic error+retry, empty-columns CTA stub (button disabled — wired by issue 82), and the success branch — Trello-style horizontal column strip (`display: flex; overflow-x: auto; min-width: 280px` per card, `#f4f5f7` background). Each column header renders name + a "0 tasks" placeholder (true count wired by issue 91). View parses `route.params.id` defensively — non-numeric / non-positive ids short-circuit to `not_found` without burning a server roundtrip; `/boards/foo` and `/boards/-3` both reach the same dead-end UX as a real 404. Boards-list rows already linked to `/boards/{id}` via `<RouterLink>` since PR 144 (the comment said "the link will resolve once /boards/:id is added — until then it 404s on click") — no list-view change needed; the wiring just started working. Store extended: `getById(id)` action with separate `currentBoard` / `currentBoardLoading` / `currentBoardError` slots so list-load and detail-load lifecycles never collide; 404s map to the literal `'not_found'` string so the view branches deterministically without parsing the message. `BoardDetail` / `Column` / `Label` interfaces added to `types/boards.ts` mirroring backend `BoardDetailRead` / `ColumnRead` / `LabelRead`. New `getBoardDetailApi(id)` wrapper. Four new vitest cases on `boards.spec.ts` (now 13 — was 9): hydrate-success + array-order assertion, 404 → `'not_found'` mapping, non-404 error captures `detail` and clears stale board, in-flight loading flag. Mandatory Playwright spec at `frontend/tests/e2e/board-view.spec.ts` (NEW file, 2 scenarios — hybrid pattern: `registerViaUi` then `page.context().request` for board+column setup) — happy-path asserts both columns visible AND DOM-order matches creation order via reading `data-testid` off `.column-strip > li` (defensive against any future flex-direction change), 404 path asserts the dedicated not-found UI + back-link. Full e2e suite green (32 specs).

**Phase 2 issue queue continues**. With 81 closed, the columns-rendering UI surface is in place. Loop continues with issue 82 (`feat(frontend): add-column button + flow`) — wires the empty-state CTA stub from #81 and adds a header "+ Add column" button. Then 90 (`POST /api/boards/{id}/tasks`) which unblocks the deferred 409 smoke scenario in `columns.spec.ts` (`TODO(#90)` breadcrumb still in place) and the rest of the Phase 2 task-CRUD chain (#91-#93).

Merged to `main` (recent — earlier history in `git log`):
- Issue 81 / PR (this) — `feat(frontend): render columns on board detail route`. New `/boards/:id` route mounted on `BoardDetailView.vue`. Five UI states (loading / not-found / error / empty-columns / columns). Trello-style horizontal strip with name + task-count placeholder per column. Store gains `getById()` + separate `currentBoard*` slots; 404 maps to literal `'not_found'`. Defensive route-param parsing (non-numeric → not-found without server roundtrip). New `BoardDetail` / `Column` / `Label` types mirror backend. Four new vitest cases. NEW Playwright spec `frontend/tests/e2e/board-view.spec.ts` (2 scenarios) — hybrid `registerViaUi` + `page.context().request` setup. 32 e2e specs total, all green.
- Issue 80 / PR — `feat(api): POST /api/boards/{id}/columns/reorder`. New `reorder_columns` service + sub-resource route. New `ColumnsReorderRequest` schema with `min_length=1`. Set-equality + length-parity validation collapses partial / extra / duplicate / unknown onto one `invalid_reorder` 400. Position rewrite to canonical 1000/2000/3000... `refresh(["updated_at"])` MissingGreenlet trap fix. 10 pytest cases. Spec extended to 8 scenarios.
- Issue 79 / PR — `feat(api): DELETE /api/columns/{id}`. New `delete_column` service + flat-shape route. Hand-built `JSONResponse` for 409 to land flat body shape `{"detail": "column_has_tasks", "task_count": N}`. 8 pytest cases. Spec extended to 6 scenarios. 409 smoke deferred to #90 with TODO breadcrumb.
- Issue 78 / PR — `feat(api): PATCH /api/columns/{id}`. New `update_column` service + flat-shape route. Introduces `ColumnError` class. `exclude_unset` clear semantics + `refresh(["updated_at"])` MissingGreenlet trap fix. 12 pytest cases. Spec extended to 4 scenarios.
- Issue 77 / PR — `feat(api): POST /api/boards/{id}/columns`. New `app/api/columns.py` router, new `app/services/columns_service.py`, repo gains `max_position_for_board()` + `create()`. Position math `MAX(position) + 1000`. 9 pytest cases; new Playwright spec `frontend/tests/e2e/api/columns.spec.ts` (2 scenarios).
- Issue 76 / PR — `feat(frontend): archive board UI`. Per-row inline Archive button; new reusable `ConfirmDialog.vue`; store `archive(id)` action with `archiving: number | null` flag. Six scenarios on `boards-list.spec.ts`. Three new vitest cases.
- Issue 75 / PR — `feat(frontend): create-board modal`. Reusable `CreateBoardModal.vue`, `createBoardApi` wrapper, store `create()` action, "New board" header button. Three Playwright scenarios on `boards-list.spec.ts`.
- Issue 74 / PR — `feat(frontend): boards list view at /boards`. Route, store, view, types, api wrapper, vitest store sanity, page-driven Playwright spec.
- Issue 73 / PR — `POST /api/boards/{id}/archive`. Idempotent (preserves original timestamp on repeat), no cascade. 7 pytest cases.
- Issue 72 / PR — `PATCH /api/boards/{id}`. `exclude_unset` semantics for `null = clear`; archived = 404; `await session.refresh(board, ["updated_at"])` to avoid `MissingGreenlet`.
- Issue 71 / PR — `GET /api/boards/{id}`. New `BoardDetailRead` schema with eager-loaded columns + labels via `selectinload`; ≤3 SELECTs locked by N+1 assertion.
- Issue 70 / PR 140 — `GET /api/boards`. `include_archived: bool = False` query param.
- Issue 69 / PR 139 — `POST /api/boards`. Router mounted under `/api`; commit owned by service per ADR-0001.
- Issue 36 / PR 137 — Phase 2 data layer kickoff. Five models, migration, read-only repositories, stubbed services, domain schemas.

Full pre-Phase-2 history in `git log`.

ADRs in force: 0001 (api+bot split with shared services), 0002 (Redis pub/sub realtime bus), 0003 (Telegram linking via one-time code), 0004 (float position for task ordering), 0005 (opaque rotating refresh tokens), 0006 (Playwright e2e + smoke-tester agent), 0007 (autonomous pre-merge review loop), 0008 (Playwright spec mandatory on every task).

**Convention reminder**: write bare issue references like "issue 23" in `followup.md` rather than `#23` — GitHub's keyword scanner interprets literal `#NN` in merge commits as closing references, which previously auto-closed issue 23 without a fix. PR bodies use `#N`; `followup.md` uses bare numbers.

## Next

1. **Loop auto-picks issue 82** — `feat(frontend): add-column button + flow`. Wires the empty-state CTA stub from #81 (`board-detail-add-first-column` is currently disabled) and adds a header "+ Add column" entry point. New `CreateColumnModal.vue` (reusable per-board) + `createColumnApi` wrapper + boards store action `addColumn(boardId, payload)`. After success, refresh `currentBoard` (via `getById`) so the new column appears in position order. Spec extends `frontend/tests/e2e/board-view.spec.ts` — open modal → submit → new column visible in strip. Optimistic UI deferred (Phase 3 realtime owns the "I just added a column on another tab" propagation).

2. **After 82 closes, loop continues to issue 90** — `feat(api): POST /api/boards/{id}/tasks`. Unblocks the deferred 409 smoke scenario in `columns.spec.ts` (the `TODO(#90)` breadcrumb is still in place — once #90 ships, extend the spec to seed a task and verify DELETE → 409 over HTTP). Also unblocks the rest of the Phase 2 task-CRUD chain (#91-#93). Once #91 (`GET /api/boards/{id}/tasks`) lands, hydrate the "0 tasks" placeholder in `BoardDetailView.vue`'s column header with the real count.

3. **Then issue 83** — `feat(frontend): rename column inline`. Inline-edit on the column header (`board-detail-column-{id}-name`) → PATCH `/api/columns/{id}`. Reuses the column-store action pattern from #82.

4. **Deferred user actions** (none gate the loop):
   - Flip `e2e` CI job to branch-protection required check after 2-3 more green runs: `gh api -X PATCH repos/vaporphd/scrumban/branches/main/protection/required_status_checks -F 'strict=true' -f 'contexts[]=backend' -f 'contexts[]=frontend' -f 'contexts[]=e2e'`.
   - Consider a server-side equivalent of the docs-only guard — a GitHub ruleset or stricter branch protection — since `--no-verify` bypasses the client hook. Low priority while Alex is solo admin.
   - Add a global toast/notification system once 2-3 flows need it (deferred from issue 75 — currently only one flow uses inline errors).
   - "Show archived" toggle on boards list view — deferred from issue 76; lands when board-restore UI ships or when bot/web sync needs to surface archived boards.
   - Consider an architectural-review checkpoint after each Tier completion (~5-10 PRs) if cross-PR drift becomes a concern.
